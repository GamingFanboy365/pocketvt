# --- Local reference material: never commit ---
# ROMs (test carts, controls, packaged play ROMs)
*.nes
*.unf
*.unif
testroms/
# NESdev wiki XML exports and NintendulatorNRS / Furbtendulator source snippets
reference/
NESdev_Wiki-*.xml
# Reference screenshots / captures
*.png
!src/**/*.png

# --- Build output ---
build/
pvt_build/
*_build/
*.o
*.d
*.elf
*.map
*.gba
*.sav
play_me.gba
# ...except the committed core at the repo root, rebuilt by CI on every PR
# (.github/workflows/build.yml)
!/pocketvt.elf
!/pocketvt.gba

# --- Editor / merge leftovers ---
*.mine_bak
*.orig
*.rej
*~
*.swp
.DS_Store
__pycache__/
